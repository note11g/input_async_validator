name: input_async_validator
description: "simple text field validator that support async validation"
version: 0.0.1-alpha.3
repository: https://github.com/note11g/input_async_validator

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  async: ^2.11.0
  rxdart: ^0.28.0 # todo: remove

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

